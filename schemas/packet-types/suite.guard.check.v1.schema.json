{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "suite.guard.check.v1.schema.json",
  "title": "Packet Type Contract: suite.guard.check.v1",
  "type": "object",
  "required": ["schema_version", "packet_type", "packet"],
  "properties": {
    "schema_version": {
      "const": "suite.packet.v1"
    },
    "packet_type": {
      "const": "suite.guard.check.v1"
    },
    "packet": {
      "type": "object",
      "required": ["version", "tool", "kind", "hash", "summary", "budget_cost", "provenance", "payload"],
      "properties": {
        "payload": {
          "type": "object",
          "required": ["passed","findings"],
          "properties": {
            "debug": {},
            "truncated": {},
            "returned_count": {},
            "total_count": {},
            "artifact_handle": {}
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  },
  "x-required_payload_fields": ["passed","findings"],
  "x-optional_payload_fields": ["debug","artifact_handle"],
  "x-boundedness_rules": ["findings array compacted in compact profile"],
  "x-compatibility_notes": ["legacy audit-only output supported for one release"]
}
